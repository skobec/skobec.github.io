<?foreach($this->data->list_fields->{$code} as $dic) {?>
    <select class="list" name="form[{$code}][]" id="field_list-{$code}">
        <option value="" >Не выбрано...</option>
        <?foreach($dic as $field) {?>
            <option <?=(array_key_exists($field->id, $this->data->list_fields->{$code}) || ($this->data->values && property_exists($this->data->values, '{$code}') && ($this->data->values->{$code} == $field->id))) ? 'selected' : '' ?> value="<?=$field->id?>"><?=htmlspecialchars($field->title)?></option>
        <?}?>
    </select> {$help}
<?}?>