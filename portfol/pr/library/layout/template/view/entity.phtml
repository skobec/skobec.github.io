<?
    $lst82 = (array)$this->data->entity_fields->{$code};
?>

<div class="controls-selectbox">
    <select name="form[{$code}][]">
        <option value="" >Не выбрано...</option>
        <?foreach($lst82 as $item) {?>
            <option <?=(($this->data->values && property_exists($this->data->values, '{$code}') && ($this->data->values->{$code} == $item->id))) ? 'selected' : '' ?> value="<?=$item->id?>"><?=htmlspecialchars($item->title)?></option>
        <?}?>
    </select> {$help}
</div>

