<?if(defined('READ_ONLY_MODE')) {?>
    <?if($this->data->values && property_exists($this->data->values, '{$code}') && ($this->data->values->{$code} !== null)){?>
        <span class="font-read-only"><?=htmlspecialchars($this->data->values->{$code})?></span>
    <?}else{?>
        <span class="font-no-data"><?=Constant::VAR_TEXT_NO_DATA?></span>
    <?}?>
<?}else{?>
    <input name="form[{$code}]" class="{$validator}" type="text" id="field-{$code}" <?if($this->data->values && property_exists($this->data->values, '{$code}') && ($this->data->values->{$code} !== null)){?>value="<?=htmlspecialchars($this->data->values->{$code})?>"<?}?> /> {$help}
<?}?>