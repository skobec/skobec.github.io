
    <div class="toolbar-panel pxs">
        <table>
            <tr>
                <td><span class="head">Электронный паспорт многоквартирного дома</span></td>
                <td>Адрес текущего дома</td>
                <td><a href="/list/">к списку домов</a></td>
                <td class="bl">&nbsp;</td> <!-- Должен быть, если есть счетчик процентов -->
            </tr>   
        </table>
    </div>

    <div class="forms">
        <h3 class="formh"></h3>
        <form class="form-horizontal">
            <?foreach($this->data->fields as $field) {?>
                <div class="control-group">
                    <label class="control-label" for="field-<?=$field->code?>"><?=htmlspecialchars($field->title)?></label>
                    <div class="controls">
                        <?switch($field->type){
                        case 6:?><!-- date -->
                            
                        <?break;
                        case 3:?><!-- text -->
                            <textarea cols="64" rows="8" name="form[<?=$field->code?>]"></textarea>
                        <?break;
                        default:?>
                            <input name="form[<?=$field->code?>]" type="text" id="field-<?=$field->code?>">
                        <?break;
                        }?>
                    </div>
                </div>
            <?}?>
            <div class="control-group">
                <label class="control-label"></label>
                <div class="controls">
                    <a class="btn btn-success" onclick="return $(this).closest('form').submit();">Сохранить</a>
                </div>
            </div>            
        </form>
    </div>

    <div class="dashed"></div>
    <br />
