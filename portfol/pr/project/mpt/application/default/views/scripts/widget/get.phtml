<?

switch($this->widget->type) {
    case 'map': {
        $this->renderWidgetMap($this->widget);
        break;
    }
    default: {
        $this->renderWidget($this->widget);
        break;
    }
}